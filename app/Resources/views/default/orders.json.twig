[
        {% for order in orders %}
        {
            "name": "{{ order.dish.name }}",
            "quantity": "{{ order.quantity }}"
        }{% if not loop.last %},{% endif %}        
        {% endfor %}
]