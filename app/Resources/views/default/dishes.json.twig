[
        {% for dish in dishes %}
        {
            "id": "{{ dish.id }}",
            "image": "{{ dish.image }}",
            "name": "{{ dish.name }}",
            "description": "{{ dish.description }}"
        }{% if not loop.last %},{% endif %}        
        {% endfor %}
]